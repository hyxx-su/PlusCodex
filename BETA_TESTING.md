# PlusCodex 다중 AI 베타

macOS 14 이상 · Apple Silicon(M1 이상)용입니다. Intel Mac은 지원하지 않습니다.
이 ZIP은 정식 릴리즈가 아니며 자동 업데이트는 꺼져 있습니다.
Developer ID 서명/공증이 없는 테스트 빌드이므로 macOS에서 실행을 차단할 수 있습니다.
신뢰할 수 있는 배포자에게 받은 파일인지 먼저 확인하세요.

## 설치

1. 기존 PlusCodex를 메뉴의 `PlusCodex 종료`로 종료합니다.
2. 기존 앱을 보관한 뒤 ZIP의 PlusCodex.app을 응용 프로그램에 복사합니다.
3. 세 AI의 기본 설정은 켜져 있지만 Claude Code와 Grok은 CLI 설치가 확인될 때만 메뉴바에 표시됩니다.
   공간이 부족하면 macOS 노치 뒤에 아이콘이 가려질 수 있습니다.

## 확인할 항목

- beta.3: 이전 앱을 완전히 종료하고 실행하세요. 5분 이상 둔 상태에서 키체인 승인창이 반복되지 않는지 확인합니다.
- 키체인을 대화상자 없이 읽지 못하면 CLI로 대체 조회합니다. Claude 조회 실패도 5분 간격으로 재시도합니다.
- 로그인 자동 실행은 첫 실행 시 기본 등록하고 이후 OFF 선택은 유지합니다. macOS 추가 승인이 필요한 경우 시스템 설정에서 허용하세요.
- 설정의 AI·자동 실행 토글과 알림 설정 버튼을 확인합니다.

- 처음 설치한 사용자: 설정의 세 체크박스가 모두 켜져 있는지 확인합니다.
- 아이콘 우클릭 → 해당 AI 끄기: 아이콘이 사라지고 설정 체크도 해제되는지 확인합니다.
- 설정에서 다시 켜기, 재실행 후 설정 유지, 모두 끈 뒤 앱 재실행으로 설정 복구를 확인합니다.
- 각 AI 메뉴에서 `설정…`과 `PlusCodex 종료`가 작동하는지 확인합니다.
- Claude Code: 로그인된 구독 계정에서 5시간·주간·모델별 한도가 서버에서 제공되는 만큼 표시되는지 확인합니다.
- Grok CLI: 로그인 후 메뉴에서 주간 또는 월간 한도와 초기화 시간을 실제 계정 화면과 비교합니다.
- 한 AI가 미설치/미로그인 상태여도 다른 AI의 조회가 정상인지 확인합니다.
- 네트워크를 끊고 다시 연결했을 때 오프라인 표시와 복구를 확인합니다.

## 조회 방식과 제한

- Claude Code의 기존 키체인 또는 `.claude/.credentials.json` OAuth 로그인을 읽어 사용량을 조회합니다.
  파일 기반 로그인 키체인에서도 백그라운드 승인창을 금지합니다. 접근 권한이나 비밀번호는 변경하지 않습니다.
  OAuth 인증 거부 또는 키체인 조회 실패 시 숨겨진 CLI의 `/usage`로 대체 조회합니다.
  Claude가 인증을 갱신하면 다시 읽어 OAuth를 재시도하며, PlusCodex가 직접 토큰을 수정하지 않습니다.
  요청 제한·명시적 권한 부족은 대체 조회로 우회하지 않습니다.
  CLI 조회는 최대 25초로 제한하고 종료합니다. 일반 채팅 메시지는 보내지 않습니다.
- CLI 최초 설정·신뢰 확인이 필요한 경우 자동 승인하지 않습니다.
  본인이 경로를 확인한 뒤 터미널에서 다음 명령으로 초기 설정을 마치고 다시 조회하세요.

  ```sh
  cd "$HOME/Library/Application Support/PlusCodex/usage-probe"
  claude
  ```

  `/usage`가 표시되는지 확인한 뒤 CLI를 종료하면 됩니다. 이 폴더는 대체 조회를 시도할 때 생성됩니다.
  CLI 대체 조회만 성공하면 이메일·플랜은 제공되지 않을 수 있고, 초기화 시간은 CLI 원문으로 표시됩니다.
- Grok CLI의 `.grok/auth.json`을 읽습니다. 웹사이트 로그인만으로는 연결되지 않습니다.
  토큰이 만료됐다면 Grok CLI를 실행해 인증을 갱신한 뒤 다시 확인하세요.
- 미설치 AI는 설정에 `미설치 · 메뉴바에서 숨김`으로 표시합니다. 설정을 열거나 60초마다 설치를 다시 확인합니다.
- API 키 결제 계정, 일부 팀 계정 및 사용량 API가 없는 계정은 퍼센트가 제공되지 않을 수 있습니다.
- Claude는 정상 조회 후 최소 5분, Grok은 최소 1분 간격으로 조회합니다.
  새로고침과 메뉴 열기도 이 간격을 따르며, 서버 요청 제한 응답의 대기시간을 존중합니다.
- 인증 정보는 앱/ZIP에 포함하거나 로그에 기록하지 않습니다. 해당 AI 서버에만 메모리상 토큰을 사용합니다.
- 기존 Codex 알림은 유지됩니다. Claude/Grok의 완료·임계치 알림은 이번 베타 범위에 포함되지 않습니다.
- 파서와 설정/UI는 로컬 테스트했지만 Claude/Grok 실제 계정 연동은 베타 테스터의 확인이 필요합니다.

문제 제보에는 macOS 버전, AI/플랜, 재현 순서, 개인정보를 가린 화면만 보내주세요.
인증 파일, 토큰, 키체인 내용은 보내지 마세요.

정식 버전으로 돌아가려면 베타 앱을 종료하고 보관한 정식 앱으로 교체하세요.
